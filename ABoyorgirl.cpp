#include <iostream>
#include <bits/stdc++.h>
using namespace std;
int main()
{
    string aux;
    set<char> s;
    cin>>aux;
    for(int i=0; i<)
    {
        s.insert(aux[i]);
    }
    return 0;
}